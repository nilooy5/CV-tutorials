clear variables;
close all;
clc;

myImg = imread("chocolate_original.jpg");
myImg = imresize(myImg,[480 640]);
[r,g,b] = imsplit(myImg);

choc_gray=rgb2gray(myImg);
figure(1);
imshow(choc_gray);

% level=graythresh(choc_gray);
% BW = imbinarize(choc_gray,level);
% figure(3);
% imshowpair(choc_gray,BW,'montage');

% figure(2);
% imhist(choc_gray);
figure(2);
imhist(b);

t1 = imbinarize(b,36/255);
figure(3);
imshow(1)